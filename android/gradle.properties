org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Pinned explicitly (2026-08-08) - this machine has two real JDKs
# installed (Microsoft JDK 17 on JAVA_HOME/PATH, and Android Studio's
# bundled JBR, which is JDK 21) and Gradle auto-detection was
# inconsistently picking up the JBR between sessions, causing a real
# "jdkImage" transform failure compiling firebase_core (see
# OPEN_QUESTIONS.md). AGP 9.0.1 + Gradle 9.1.0 (see gradle-wrapper.
# properties) both officially target JDK 17 as the build JVM - pinning
# here removes the ambiguity rather than hoping auto-detection picks the
# right one. Update this path if the Microsoft JDK is ever reinstalled
# at a different version/location.
org.gradle.java.home=C:\\Program Files\\Microsoft\\jdk-17.0.19.10-hotspot
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
