name: commuteos
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # MTA GTFS-realtime feed fetching and parsing
  http: ^1.5.0
  gtfs_realtime_bindings: ^1.2.0

  # Parsing the bundled MTA static station list (assets/data/mta_stations.csv)
  csv: ^6.0.0

  # Local on-device persistence for favorite stations (Phase 1: no backend),
  # and remembering the smart-notification toggle's on/off state locally.
  shared_preferences: ^2.5.3

  # Real push notification delivery (see push_registration_service.dart) -
  # firebase_core initializes the app from android/app/google-services.json;
  # firebase_messaging gets the real device token the backend sends pushes
  # to (see backend/app/notify_service.py).
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.6

  # Real device GPS coordinates for Chat AI's location-dependent questions
  # ("nearest station to me") - see lib/chat/location_service.dart. Only
  # ever read on-demand right before sending such a question, never polled
  # in the background.
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Only used directly in test/lirr_service_test.dart to build a fake
  # GTFS-RT feed's Int64 `time` field - already a transitive dependency
  # via gtfs_realtime_bindings, declared explicitly since analyze flags
  # relying on a transitive-only package as fragile.
  fixnum: ^1.1.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generate the Android launcher icon (flutter_launcher_icons) and native
  # splash screen (flutter_native_splash) from the assets in assets/icon/ -
  # see the flutter_launcher_icons:/flutter_native_splash: config sections
  # below. Both are one-shot code generators (`dart run <package>:main`),
  # not runtime dependencies of the app itself.
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/data/mta_stations.csv
    - assets/data/njt_rail_stations.csv
    - assets/data/njt_bus_stops.csv
    - assets/data/lirr_stations.csv
    - assets/data/lirr_trip_routes.csv
    - assets/icon/icon_master.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Generates android/app/src/main/res/mipmap-*/ic_launcher.png (legacy) and
# the adaptive icon (foreground/background layers, mipmap-anydpi-v26/) from
# these two source images - see assets/icon/ for the SVG sources they were
# rendered from. Run `dart run flutter_launcher_icons` to regenerate after
# editing either PNG.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon_master.png"
  adaptive_icon_background: "#1c150f"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

# Generates the native splash screen (shown instantly on cold start, before
# the Flutter engine/first frame is ready) from the same brand assets -
# matches CommuteOS's own dark theme rather than the Flutter default white
# screen. Run `dart run flutter_native_splash:create` to regenerate.
flutter_native_splash:
  color: "#1c150f"
  image: "assets/icon/icon_foreground.png"
  android_12:
    color: "#1c150f"
    image: "assets/icon/icon_foreground.png"
  android: true
  ios: false
